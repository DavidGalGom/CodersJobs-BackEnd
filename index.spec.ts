describe("Given a project with gitHub workFlows", () => {
  describe("When a branch is going to get merged", () => {
    test("Then a successful test is needed", () => {
      expect(true).toBe(true);
    });
  });
});
